#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

int main(__attribute__ ((unused)) int argc, char* argv[])
{

	if (argc < 3 || argc > 3)
	{
		printf("Error\n");
		return (1);
	}
		printf("%d\n", atoi(argv[1]) * atoi(argv[2]));
		return (0);
}
